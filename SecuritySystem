using System.Collections;
using UnityEngine;

[RequireComponent(typeof(AudioSource))]
public class SecuritySystem : MonoBehaviour
{
    private AudioSource _audioSourse;

    private void Start()
    {
        _audioSourse = GetComponent<AudioSource>();
    }

    public void TurnDownVolume()
    {
        StopCoroutine(TurnUpTheVolume());
        StartCoroutine(TurnDownTheVolume());
    }

    public void TurnUpVolume()
    {
        StopCoroutine(TurnDownTheVolume());
        StartCoroutine(TurnUpTheVolume());
    }

    private IEnumerator TurnDownTheVolume()
    {
        while (_audioSourse.volume != 0f)
        {
            _audioSourse.volume = Mathf.MoveTowards(_audioSourse.volume, 0.0f, Time.deltaTime);
            yield return null;
        }

    }

    private IEnumerator TurnUpTheVolume()
    {
        _audioSourse.Play();
        while (_audioSourse.volume != 1f)
        {
            _audioSourse.volume = Mathf.MoveTowards(_audioSourse.volume, 1.0f, Time.deltaTime);
            yield return null;
        }
    }
}
